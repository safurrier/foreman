# Implementation Plan

1. Replace search overlay rendering with footer-based search status.
2. Add labeled footer hint groups for scanability.
3. Update help legend for harness glyph + status-colored pane labels.
4. Use elided window titles for direct singleton pane rows.
5. Update render/runtime tests and SPEC.
6. Run `mise run check`, `mise run verify-ux`, push, and poll CI.
